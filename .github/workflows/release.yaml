name: Release

on:
  push:
    tags:
      - v**

jobs:
  release:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Set up JDK 17
        uses: actions/setup-java@v3

        with:
          java-version: '17'
          distribution: 'adopt'
      - name: Build with Maven
        run: echo ${{ inputs.tags }} && mvn versions:set -DnewVersion=${{ inputs.tags }} && run mvn clean deploy -DskipTests=true -B -U
        
